"""This module/file holds some functionality"""

def add_one(number: int = 0) ->int:
    """Example Functions which adds one to the input
    
    Args: 
        number (int): input number where 1 is added 
            Dfaulst to 0

    Example:
        >>> from example_package_ST import example
        >>> example.add_one(5)
        6

        >>> example.add_one()
        1  
            

    Returns:
        integer which is input + 1

    """
    return number + 1